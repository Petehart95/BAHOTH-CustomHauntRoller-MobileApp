<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HauntRoller"
             x:Class="HauntRoller.SettingsPage" NavigationPage.HasBackButton="False">
    <NavigationPage.TitleView>
        <Button x:Name="SaveButton" 
                        Text="Save" 
                        Clicked="SaveClicked_Handler"
                        Margin="0"
                        HorizontalOptions="End"
                        Background="transparent"/>
    </NavigationPage.TitleView>
    
    <StackLayout Margin="0,20,0,0">
        <Label Text="Settings" Margin="20" FontSize="Medium"></Label>
        <StackLayout Orientation="Horizontal">
            <Label Text="Dice Enabled" Margin="20"/>
            <Button x:Name="DiceToggleButton" Text="On" Clicked="DiceToggleButtonClicked_Handler" />
        </StackLayout>
    </StackLayout>
</ContentPage>